tasty_bytes:
  target: dev
  outputs:
    dev:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: DBT_DEMO_DB
      schema: dev
      warehouse: DEMO_WH
    prod:
      type: snowflake
      account: 'not needed'
      user: 'not needed'
      role: accountadmin
      database: DBT_DEMO_DB
      schema: prod
      warehouse: DEMO_WH